# Copy to .env.local for local builds (never commit real values).
# GA4 is baked in at Vite build time — set on EC2 before `npm run build` / commitreferrall.sh.
# VITE_GA4_MEASUREMENT_ID=G-XXXXXXXXXX
