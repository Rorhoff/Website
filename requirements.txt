# Python deps for FastAPI backend (pip install -r requirements.txt).
# Add packages here when extending main.py or routers; pin versions for production deploys.

fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
sqlalchemy>=2.0.36
psycopg[binary]>=3.2.0
passlib[bcrypt]>=1.7.4
# passlib 1.7.x is incompatible with bcrypt 4.1+ (version probe + wrap-bug self-test).
bcrypt>=4.0.1,<4.1
python-dotenv>=1.0.1
httpx>=0.27.0
# TLS/browser impersonation for tools/sync_craigslist_classifieds.
curl_cffi>=0.7.0
# S3-compatible object storage client (Cloudflare R2, AWS S3). Used by image_storage.py.
boto3>=1.34.0
# Stripe SDK for the gold-frame paywall (Checkout + webhook signature verification).
stripe>=10.0.0
pypdf>=5.0.0
pytest>=8.0
pytest-asyncio>=0.23
